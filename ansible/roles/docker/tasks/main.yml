---
- name: Detect Docker CLI
  ansible.builtin.command: docker --version
  register: docker_check
  changed_when: false
  failed_when: false
  check_mode: false

- name: Detect Docker Compose plugin
  ansible.builtin.command: docker compose version
  register: compose_check
  changed_when: false
  failed_when: false
  check_mode: false
  when: docker_check.rc == 0

- name: Require Docker and Docker Compose
  ansible.builtin.assert:
    that:
      - docker_check.rc == 0
      - compose_check.rc | default(1) == 0
    fail_msg: 'Docker and the Docker Compose plugin are required.'
    success_msg: 'Docker and Docker Compose are available.'

- name: Detect installed LazyDocker version
  ansible.builtin.command:
    argv:
      - "{{ lazydocker_binary_path }}"
      - --version
  register: lazydocker_check
  changed_when: false
  failed_when: false
  check_mode: false

- name: Report LazyDocker status
  ansible.builtin.debug:
    msg: >-
      LazyDocker {{ lazydocker_version }}
      {{ 'will be installed.' if lazydocker_check.rc != 0 or lazydocker_version not in lazydocker_check.stdout else 'is available.' }}
  changed_when: >-
    ansible_check_mode and
    (lazydocker_check.rc != 0 or lazydocker_version not in lazydocker_check.stdout)

- name: Install LazyDocker
  ansible.builtin.include_tasks: install_lazydocker.yml
  when:
    - not ansible_check_mode
    - lazydocker_check.rc != 0 or lazydocker_version not in lazydocker_check.stdout
